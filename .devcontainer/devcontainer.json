{
  "name": "RTL Demo",
  "image": "mcr.microsoft.com/devcontainers/cpp:ubuntu",
  "postCreateCommand": "cmake -S . -B build && cmake --build build && ./bin/RTLDemoApp && sleep 5 && ./scripts/download-latest-binaries.sh",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.cpptools"]
    }
  }
}
